name: Flutter Web
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build Web
    env:
      commit-token: ${{secrets.COMMIT_SECRET}}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
        with:
          channel: stable
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release --base-href /flutter_portfolio/
      - run: |
          cd build/web
          git init
          git config --global user.email theprocraft@gmail.com
          git config --global user.name Rukkaitto
          git status
          git remote add origin https://${{env.commit-token}}@github.com/Rukkaitto/flutter_portfolio.git
          git checkout -b gh-pages
          git add --all
          git commit -m "update"
          git push origin gh-pages -f